groupId=de.dbck.poc
artifactId=poc-artifactId
version=0.0.1-SNAPSHOT
package=${groupId}.${artifactId}
description=Project ${groupId}-${artifactId}
url=https://github.com/dbck/${project.artifactId}
scmConnection=scm:git:${project.scm.url}.git
